// Configuración global de Vitest. Ver .doc/rules/tests.md.
// Vitest fija NODE_ENV='test'. Aquí irían mocks/stubs globales si se requieren
// (p.ej. ioredis-mock para Valkey en pruebas funcionales con fastify.inject).
