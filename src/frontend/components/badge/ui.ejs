<%
  const label = locals.label || '';
  const variant = locals.variant || 'neutral'; // success | warning | error | info | neutral
%>
<span class="c-badge c-badge--<%= variant %>"><%= label %></span>
