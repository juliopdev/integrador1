<%
  // Badges vía include() (devuelve string) → la sección no escribe la clase c-badge a mano.
  const okBadge = include('frontend/components/badge/ui', { variant: 'success', label: 'Conectado' });
  const stableBadge = include('frontend/components/badge/ui', { variant: 'info', label: 'Estable' });
  const healthBody = `
    <ul class="c-health-list">
      <li class="c-health-item"><span class="c-health-item__name">Base de Datos Platform</span>${okBadge}</li>
      <li class="c-health-item"><span class="c-health-item__name">Servicio Valkey (Cache/Session)</span>${okBadge}</li>
      <li class="c-health-item"><span class="c-health-item__name">Estado del Host</span>${stableBadge}</li>
    </ul>`;
%>
<%- include('frontend/components/card/ui', { title: 'Estado del Sistema', body: healthBody, padding: 'md' }) %>
