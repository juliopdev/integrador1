<%
  const token = locals.token || null;
%>
<%- include('frontend/components/form/ui', {
  class: 'l-auth-card',
  method: 'post',
  action: '/api-system/v1/login',
  redirect: token ? '/dashboard/login?activated=1' : '/dashboard',
  body: include('backend/feature/auth-admin/presentation/views/sections/_form-login-body', locals)
}) %>
