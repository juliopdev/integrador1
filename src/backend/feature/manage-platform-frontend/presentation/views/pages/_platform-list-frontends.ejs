<%
  // Página delgada (views.md §2, §7): frame + sección. Sin `action` → sin botón arriba
  // (los frontends se configuran DESPUÉS de aprovisionar el tenant, no desde acá).
%>
<div class="u-flex u-flex-col u-gap-md">
  <%- include('backend/common/views/sections/_dashboard-page-frame', {
    breadcrumb: [
      { label: 'Inicio', href: '/dashboard' },
      { label: 'Frontends' },
    ],
    title: 'Frontends de los tenants',
    subtitle: 'Configura cómo se sirve el frontend de cada subdominio: hospedado (interno) o redirect a un dominio externo.',
    currentPath: locals.currentPath,
  }) %>
  <%- include('backend/feature/manage-platform-frontend/presentation/views/sections/_list-frontends', locals) %>
</div>
